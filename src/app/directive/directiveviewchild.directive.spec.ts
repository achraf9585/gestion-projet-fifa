import { DirectiveviewchildDirective } from './directiveviewchild.directive';

describe('DirectiveviewchildDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveviewchildDirective();
    expect(directive).toBeTruthy();
  });
});
